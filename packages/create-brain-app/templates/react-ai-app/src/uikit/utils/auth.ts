// import { settings } from '@/base/kernel/Settings';

export const redirectToLogin = () => {
  // const currentUrl = encodeURIComponent(`${window.location.href}`);
  // window.location.href = `${settings.imagicaUrl}/signin?redirect=${currentUrl}`;
  console.log(
    '可以使用这个地址来进入项目 http://localhost:3900/reactflow/?token=[你的BrainToken]'
  );
};
